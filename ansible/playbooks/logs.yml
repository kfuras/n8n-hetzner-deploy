---
- name: View service logs
  hosts: all
  become: true
  gather_facts: false

  tasks:
    - name: Show logs for {{ service }}
      ansible.builtin.raw: |
        cd {{ stack_dir }}
        docker compose logs --tail=100 {{ service }}
      changed_when: false
